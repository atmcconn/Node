/*
Write a function which will invoke a method passed as an argument in reference 
of an object argument passed

*/


var callAndApply = {
	caller: function (object, method, nameArg, ageArg, tShirtSizeArg) {
		//Solution goes here
	}
	
	applier: function (object, method, argumentsArr) {
		// Solution goes here
	}
};

module.exports = callAndApply;