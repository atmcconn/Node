function buttonReport( btnId, btnName, btnValue ){
	// information about the id of the buttonId
	var uMsg1 = "Button id: " + btnId + "\n";
	// then about the button name
	var uMsg2 = "Button name: " + btnName + "\n";
	// then about the button value
	var uMsg3 = "Button value: " + btnValue;
	//alert the user
	alert(uMsg1 + uMsg2 + uMsg3)
};	